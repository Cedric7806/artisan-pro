export type UserSession = {
  id: string;
  nom: string;
  email: string;
  role: string;
  entreprise_id: string;
  entreprise_nom: string;
};

export type SelectOption = {
  id: string;
  label: string;
};
